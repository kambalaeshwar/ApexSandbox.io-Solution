public Integer nextPrime(Integer num) {
    while(true){
        Integer Count=0;
        Integer NP = ++num;
        for(Integer i = 2; i<NP; i++){
            if(Math.mod(NP,i)==0){
                Count++;
            }
        }
        if(Count==0){
            return NP;
        }
    }
}